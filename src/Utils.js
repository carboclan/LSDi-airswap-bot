import { validateHexString } from './utils/validateHexString';
import { wrapAsBigNumber } from './utils/wrapAsBigNumber';

export const Utils = {
  validateHexString,
  wrapAsBigNumber,
};

export default Utils;
